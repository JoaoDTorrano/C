*caracteres*
isalpha//retorna se o valor é uma letra ou n
isdigit//retorna se é um digito
toupper, tolower // retorna maiusculo ou minuscolo
isupper, islower // indentifica se a letra esta em minusculo ou maiusculo
 
*vetor de char*
strlen(c) -> tamanho de c
strcpy(c, d) -> copia d pra c
strcmp(c, d) -> se forem iguais, retorna 0
strcat(c, d)
 
*string*
.size()
c = d
c == d
c + d